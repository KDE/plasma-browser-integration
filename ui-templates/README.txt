This folder contains EJS templates for complex UI bits in the extension.

See: https://ejs.co/

To generate templates:

You need NodeJS with ejs:

$ npm install ejs

Then run the following command to generate the UI files.
They will be copied into the appropriate place in the extension folder automatically.

$ npm run generate
